/**
 * @license
 * Copyright 2025 Qwen Team
 * SPDX-License-Identifier: Apache-2.0
 */
/**
 * Hardcoded schemas for the cua-driver computer-use tools.
 *
 * Pinned to: cua-driver-rs v0.5.2 (see CUA_DRIVER_VERSION in constants.ts).
 *
 * Generated from the live `cua-driver mcp` `tools/list` output — the FULL
 * tool surface cua-driver advertises (no curation). Exposing the complete set
 * (including page/CDP, cursor, session, recording, config, kill_app, …) so the
 * model has every capability cua-driver actually provides.
 *
 * Regenerated by scripts/sync-computer-use-schemas.ts — do not hand-edit.
 */
export interface ComputerUseToolSchema {
    description: string;
    parameterSchema: Record<string, unknown>;
}
export declare const COMPUTER_USE_TOOL_NAMES: readonly ["bring_to_front", "check_for_update", "check_permissions", "click", "double_click", "drag", "end_session", "get_accessibility_tree", "get_agent_cursor_state", "get_config", "get_cursor_position", "get_recording_state", "get_screen_size", "get_window_state", "hotkey", "kill_app", "launch_app", "list_apps", "list_windows", "move_cursor", "page", "press_key", "replay_trajectory", "right_click", "scroll", "set_agent_cursor_enabled", "set_agent_cursor_motion", "set_agent_cursor_style", "set_config", "set_value", "start_recording", "start_session", "stop_recording", "type_text", "zoom"];
export type ComputerUseToolName = (typeof COMPUTER_USE_TOOL_NAMES)[number];
export declare const COMPUTER_USE_SCHEMAS: Record<ComputerUseToolName, ComputerUseToolSchema>;
